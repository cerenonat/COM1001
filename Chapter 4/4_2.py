# 4.2 Discussion: Function-Call Stack
# If you keep pushing onto the call stack without popping (returning),
# eventually you will run out of memory and get a StackOverflowError (RecursionError in Python).
