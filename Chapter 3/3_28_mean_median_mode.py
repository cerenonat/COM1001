# 3.28 Mean, Median, Mode
import statistics

data = [9, 11, 22, 34, 17, 22, 34, 22, 40]
print("Mean:", statistics.mean(data))
print("Median:", statistics.median(data))
print("Mode:", statistics.mode(data))
# Adding another 34 → multiple modes problem
